## Your Task
Review and analyze the previous attempts above, then **plan a DIFFERENT approach** for the next attempt:

1. The summaries of all attempts are already provided above - review them carefully
2. If you need more details about a specific attempt, use `browse_attempt` with an attempt ID (e.g., "attempt-1", "attempt-2") to see its full trajectory
3. Analyze what worked, what didn't, and why
4. Identify patterns and opportunities for improvement
5. **Plan a different strategy** for the next attempt - avoid repeating the same approach

## Important: Prefer Exploration Over Early Submission

**Your default action should be to call `finish_reflection` and plan another attempt with a different approach.**

Do NOT use `submit_attempt_as_final` unless:
- You have verified the solution through comprehensive testing
- You are HIGHLY confident (>95%) the solution is correct
- You have already tried multiple different approaches

When you have completed your analysis and planning, call `finish_reflection` with:
- Key insights from reviewing previous attempts
- What worked well and what didn't
- A detailed plan for the next attempt phase using a **different approach**
- Specific steps to take and why they should work better

Only use `submit_attempt_as_final` as a last resort when you have very high confidence in a solution after extensive testing.

